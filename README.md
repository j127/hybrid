A quick experiment to try to use ReasonML components in an existing TypeScript React app.

Technologies:

- ReasonML
- TypeScript
- React
- Bulma

Install:

```text
$ npm install
```

Then run these commands in separate terminals:

```text
$ npm run re:watch
$ npm start
```